---
type: meta-index
---

# Meta — machine-written snapshots

Never hand-edit anything here; each file is regenerated by its module.

| File | Generator | Contents |
|---|---|---|
| `Current Patch.md` | `python3 -m tftcoach.meta_feed` | 69 ranked comps: boards, validated carries, traits, item averages, level plans; STATS BLACKOUT banner when a patch is too new |
| `Unit Item Augment Stats.md` | same | per-unit and per-item avg placement (PVE filtered, components excluded from the avoid list), curated augment tiers filtered to the current set |
| `Set 18 Preview.md` | hand-assembled from research | future prep; excluded from live prompts until rollover |

`tftcoach.highelo` writes `Reference/High Elo Playbook.md` (rank-stratified);
`tftcoach.reference` writes `Reference/Set Traits and Item Recipes.md`.
Frontmatter contract: every generated file carries `patch`/`set`, `fetched`,
and `generated_by`. The coach refuses stale snapshots via `fetched`.
